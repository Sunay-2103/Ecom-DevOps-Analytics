-- Database initialization script
-- Run automatically by PostgreSQL on first startup

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Indexes for analytics performance (tables are created by SQLAlchemy)
-- These are applied after table creation via backend startup

-- Grant permissions
GRANT ALL PRIVILEGES ON DATABASE ecom_db TO ecom_user;
change 5
change 22
change 1
change 19
